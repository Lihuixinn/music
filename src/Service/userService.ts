import { createSlice } from "@reduxjs/toolkit";

const userService = createSlice(
    {
        name:'user',
        initialState:{id:123},
        reducers:{

        }
    }
)

export default userService;